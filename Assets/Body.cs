using UnityEngine;

public class Body : MonoBehaviour{
    Transform lfl1;
    Transform lfl2;
    public float x;
    public float y;
    public float z;

    void Start(){
        // transform.localScale = new Vector3(0.5f, 1, 0.5f);
        // lfl1 = transform.Find("Left_Front_Leg1");
        // lfl1.lossyScale = new Vector3(1f, 1, 1f);
        // lfl2 = lfl1.Find("Left_Front_Leg2");
        // x = lfl1.position.x;
        // y = lfl1.position.y;
        // z = lfl1.position.z;

        // lfl1.localScale=
    }

    // Update is called once per frame
    void Update(){
    }
}
